# allure.properties for Cucumber BDD (Java)

# Where Allure stores raw results
allure.results.directory=target/allure-results

# Where Allure will generate the final report
allure.report.directory=target/allure-reports